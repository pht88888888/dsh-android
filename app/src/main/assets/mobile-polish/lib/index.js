// @dsh-mobile/mobile-polish — host side is a no-op; all adaptation lives in
// the client half (lib/client.js). Exports follow the cordis plugin contract.
export const name = '@dsh-mobile/mobile-polish';
export const inject = [];
export function apply(ctx) {}
